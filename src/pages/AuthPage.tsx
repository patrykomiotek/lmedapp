import { AuthInfo } from "../components/Auth/AuthInfo";
import { Header } from "../ui";

export const AuthPage = () => {
  return (
    <div>
      <Header>Auth</Header>
      <AuthInfo />
    </div>
  );
};
